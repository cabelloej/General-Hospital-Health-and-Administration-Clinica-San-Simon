SELECT MATDCGE
GO TOP
DO WHILE .NOT. EOF()
   IF SUBSTR(NUMERO,1,3)<>"NOT"
      SELECT MATDCGE
      SKIP
      LOOP
   ENDIF
   STORE NUMERO TO WNUMERO2
   DO MATACTIN
   SELECT MATDCGE
   SKIP
ENDDO
RETURN
